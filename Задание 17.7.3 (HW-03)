# Дан словарь с распределением процентных ставок по вкладам в различных банках.
per_cent = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}

# На вход программы с клавиатуры вводится сумма, которую человек планирует положить под проценты.
money = input("Введите сумму которую планируете положить под проценты: ")

deposit = (list(per_cent.values()))  # Получаем список значений из словаря.
for i in range(len(deposit)):  # Получаем доступ ко всем значениям из списка.
    # Записываем в качестве нового значения накопленные средства за год.
    deposit[i] = round((deposit[i] / 100.0 * float(money)), 2)

print(deposit)  # Выводим список значений - накопленные средства за год.
# Находим максимальное значение среди элементов в списке.
print("Максимальная сумма, которую вы можете заработать:", max(deposit))  
